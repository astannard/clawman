<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-07-21T21:13:52+00:00</updated><id>/feed.xml</id><title type="html">Clawman</title><subtitle>A VS Code extension that finds every Claude context file in your project, flags the stale ones, and surfaces the contradictions.</subtitle><entry><title type="html">1.4 — he noticed the directory wasn’t there</title><link href="/blog/2026/07/09/dead-paths/" rel="alternate" type="text/html" title="1.4 — he noticed the directory wasn’t there" /><published>2026-07-09T11:00:00+00:00</published><updated>2026-07-09T11:00:00+00:00</updated><id>/blog/2026/07/09/dead-paths</id><content type="html" xml:base="/blog/2026/07/09/dead-paths/"><![CDATA[<p>Clawman has been wanting to mention this for a while.</p>

<p>Context files are full of paths. <em>“Auth logic lives in <code class="language-plaintext highlighter-rouge">src/auth/</code>.”</em> <em>“Migrations go in <code class="language-plaintext highlighter-rouge">db/migrate/</code>, never in <code class="language-plaintext highlighter-rouge">db/</code>.”</em> <em>“See <code class="language-plaintext highlighter-rouge">docs/architecture/overview.md</code> for the full picture.”</em> These are the most useful sentences in a context file, because they orient the assistant before it starts searching.</p>

<p>They’re also the sentences most likely to be quietly wrong, because directories move and nobody greps the context files afterwards.</p>

<h2 id="dead-path-detection">Dead path detection</h2>

<p>Every path mentioned in every context file now gets resolved against the actual workspace. Ones that don’t exist get flagged with the path as written and where Clawman looked:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DEAD REFERENCE · CLAUDE.md:22

  "Auth logic lives in src/auth/"

  No such path. Closest match: src/services/auth/
  (renamed in 4f2a91c, 6 weeks ago)
</code></pre></div></div>

<p>Where the git history makes it obvious — a rename in a single commit — you get the closest match and the commit that did it. Where it doesn’t, you just get told the path is gone, which is still the useful half.</p>

<p>It also checks:</p>

<ul>
  <li><strong>Commands against <code class="language-plaintext highlighter-rouge">package.json</code></strong> — a context file telling Claude to run <code class="language-plaintext highlighter-rouge">npm run lint:strict</code> when that script was removed two months ago</li>
  <li><strong>Relative links between context files</strong> — <code class="language-plaintext highlighter-rouge">.claude/</code> files referencing each other after a reorganisation</li>
  <li><strong>Config files by name</strong> — references to a <code class="language-plaintext highlighter-rouge">.eslintrc</code> that became <code class="language-plaintext highlighter-rouge">eslint.config.mjs</code></li>
</ul>

<h2 id="what-this-turned-up">What this turned up</h2>

<p>I ran it across eight repos, including three that had been through Clawman 1.3 and come out clean.</p>

<p>Every single one had at least one dead path. The median was four. The worst had nineteen, in a context file that had been rewritten wholesale after a restructure — except the restructure happened twice, and the second one wasn’t reflected anywhere.</p>

<p>The pattern is consistent: dead paths cluster in the <em>oldest and most confidently-written</em> sections of a context file. The paragraph someone sat down and composed carefully at the start of the project is exactly the paragraph describing a structure that no longer exists.</p>

<p>That’s an uncomfortable thing to learn about documentation in general, and I don’t have a tidy conclusion about it.</p>

<h2 id="also-in-this-release">Also in this release</h2>

<ul>
  <li>Conflict view now shows both excerpts without expanding, when they’re short enough to fit</li>
  <li>Added <code class="language-plaintext highlighter-rouge">clawman.alwaysArchive</code> for glob patterns that should never be flagged — for vendored or generated context files you don’t control</li>
  <li>The panel remembers scroll position between rescans</li>
  <li>Fixed: dead path detection ignoring paths inside fenced code blocks, which was the correct call about 80% of the time and is now configurable</li>
  <li>Fixed: rescanning a workspace with no context files showed a spinner indefinitely</li>
</ul>

<h2 id="next">Next</h2>

<p>The thing I keep circling is <strong>ownership</strong>. A context file with no clear author, last touched by someone who left, is a different kind of risk from one the current team actively maintains — and git already knows which is which.</p>

<p>Not sure yet whether that’s a state, a signal that feeds staleness, or just a column. If you have a view, <a href="https://github.com/your-github-username/clawman/issues">the issue is open</a>.</p>]]></content><author><name></name></author><category term="release" /><category term="detection" /><summary type="html"><![CDATA[Clawman has been wanting to mention this for a while.]]></summary></entry><entry><title type="html">1.3 — conflicts now show both sides</title><link href="/blog/2026/06/18/conflicts-side-by-side/" rel="alternate" type="text/html" title="1.3 — conflicts now show both sides" /><published>2026-06-18T10:30:00+00:00</published><updated>2026-06-18T10:30:00+00:00</updated><id>/blog/2026/06/18/conflicts-side-by-side</id><content type="html" xml:base="/blog/2026/06/18/conflicts-side-by-side/"><![CDATA[<p>The most common piece of feedback on 1.0 was some version of: <em>“okay, but which part?”</em></p>

<p>Fair. Clawman would tell you that <code class="language-plaintext highlighter-rouge">CLAUDE.md</code> and <code class="language-plaintext highlighter-rouge">.claude/skills/testing.md</code> disagreed, and then leave you to open both files and hunt for it. For a big context file that’s a genuinely annoying two minutes, and two minutes of friction is enough that people stop resolving conflicts at all.</p>

<h2 id="both-excerpts-side-by-side">Both excerpts, side by side</h2>

<p>Conflicts now expand in place. You get the actual conflicting lines from each file, with line numbers and enough surrounding context to tell whether it’s a real contradiction or two statements that merely look similar.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONFLICT · test command

CLAUDE.md:14
  Run the full suite with `npm test` before every commit.

.claude/skills/testing.md:31
  Always use `pnpm test:ci` — `npm test` skips the integration tags.
</code></pre></div></div>

<p>Both line references jump straight to the file. Resolve by editing either one, or by archiving the one that’s wrong. Clawman re-checks on save.</p>

<h2 id="four-kinds-of-conflict">Four kinds of conflict</h2>

<p>The detector now classifies what it finds, which mostly matters because it lets you ignore whole categories when they’re noisy:</p>

<ul>
  <li><strong>Command conflicts</strong> — two files, two different commands for the same job. Highest signal by a distance. This is the one that costs you real time.</li>
  <li><strong>Rule conflicts</strong> — one file says always, another says never.</li>
  <li><strong>Convention conflicts</strong> — disagreement about naming, structure, or file layout.</li>
  <li><strong>Scope overlap</strong> — two files both claiming authority over the same directory. Not always wrong, but worth a look in a monorepo.</li>
</ul>

<h2 id="sensitivity-because-the-first-scan-is-brutal">Sensitivity, because the first scan is brutal</h2>

<p>A repo that’s never been audited lights up. That’s accurate, and it’s also demoralising, and a demoralising first run is how you lose someone in the first ninety seconds.</p>

<p>So there’s now <code class="language-plaintext highlighter-rouge">clawman.conflictSensitivity</code>:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">strict</code> — flags any directive that differs, including phrasing. Noisy. Good for a deliberate one-off audit.</li>
  <li><code class="language-plaintext highlighter-rouge">balanced</code> — semantic disagreements only. The default.</li>
  <li><code class="language-plaintext highlighter-rouge">relaxed</code> — direct contradictions only, like two different values for the same command. A reasonable place to start on a big old repo, then work up.</li>
</ul>

<h2 id="also-in-this-release">Also in this release</h2>

<ul>
  <li>Conflict detection now understands that a nested <code class="language-plaintext highlighter-rouge">CLAUDE.md</code> overrides its parent for paths beneath it, rather than reporting the override as a conflict. This was the single largest source of false positives in monorepos.</li>
  <li>Findings re-check on save instead of only on rescan.</li>
  <li>Fixed: symlinked <code class="language-plaintext highlighter-rouge">.claude/</code> directories were being skipped entirely. If you symlink a shared context directory across repos, 1.0 was silently ignoring all of it.</li>
  <li>Fixed: files with CRLF line endings reported line numbers one off.</li>
</ul>

<h2 id="next">Next</h2>

<p>Dead path detection is the thing I keep wanting and it isn’t built yet. Context files describe directories constantly, and those directories move. Working on it.</p>]]></content><author><name></name></author><category term="release" /><category term="conflicts" /><summary type="html"><![CDATA[The most common piece of feedback on 1.0 was some version of: “okay, but which part?”]]></summary></entry><entry><title type="html">Clawman 1.0 — somebody has to keep the shelf tidy</title><link href="/blog/2026/05/14/hello-clawman/" rel="alternate" type="text/html" title="Clawman 1.0 — somebody has to keep the shelf tidy" /><published>2026-05-14T09:00:00+00:00</published><updated>2026-05-14T09:00:00+00:00</updated><id>/blog/2026/05/14/hello-clawman</id><content type="html" xml:base="/blog/2026/05/14/hello-clawman/"><![CDATA[<p>This started with a bad afternoon of debugging.</p>

<p>Claude had been getting steadily worse on one of our repos. Not dramatically — just enough that everyone had quietly started double-checking its output, then quietly started using it less. The assumption in the room was that something had changed on the model side.</p>

<p>Nothing had changed on the model side. What had changed was that we had, over about four months, accumulated eleven context files. Two of them gave opposite instructions about the test command. One described a directory structure we’d refactored away in March. Three had been written by someone who left in February and nobody had opened since.</p>

<p>Claude was reading all of it. Every single time. And doing an admirable job of trying to satisfy eleven documents that did not agree with each other.</p>

<h2 id="the-failure-mode">The failure mode</h2>

<p>Context rot is invisible in a way that most technical debt isn’t.</p>

<p>A broken build fails loudly. A slow query shows up in a dashboard. But a context file that stopped being true six weeks ago produces output that is subtly, unfalsifiably a bit worse — and the natural place to assign blame is the model, because the file is not something anyone is looking at.</p>

<p>Nobody reads context files. That’s not a criticism, it’s a structural fact: they’re written once, at a moment of high intention, and then they’re infrastructure. You don’t reread your <code class="language-plaintext highlighter-rouge">.gitignore</code> either.</p>

<p>The difference is that a stale <code class="language-plaintext highlighter-rouge">.gitignore</code> doesn’t quietly instruct an assistant to do the wrong thing several hundred times a week.</p>

<h2 id="what-10-does">What 1.0 does</h2>

<p>Clawman lives in your sidebar and does one unglamorous job: he finds every context file in the workspace and tells you which ones you can still trust.</p>

<ul>
  <li><strong>Discovery</strong> — <code class="language-plaintext highlighter-rouge">CLAUDE.md</code> at any depth, <code class="language-plaintext highlighter-rouge">.claude/</code> directories, skills, memory files. Including the ones you forgot about, which in our case was four of the eleven.</li>
  <li><strong>Staleness</strong> — not “this file is old,” which is useless, but “this file hasn’t changed while the code it describes has changed forty-one times.” With the date and the count, so you can disagree with him.</li>
  <li><strong>Conflicts</strong> — the expensive one. Two files both loaded into context, both authoritative, giving different instructions. You get both excerpts and a jump-to-line on each.</li>
  <li><strong>Archive</strong> — because some files are old and completely fine, and a tool that can’t take <em>no</em> for an answer is a tool you uninstall.</li>
</ul>

<h2 id="what-he-deliberately-doesnt-do">What he deliberately doesn’t do</h2>

<p>He doesn’t rewrite your context files. Deciding what’s true about your codebase is a judgement call and it isn’t his.</p>

<p>He doesn’t have opinions about your architecture. He’s a clerk.</p>

<p>He doesn’t send anything anywhere. No telemetry, no uploads, no account, no network calls at all. He reads files on disk and that is the entire surface area.</p>

<h2 id="try-it-on-a-repo-youve-had-a-while">Try it on a repo you’ve had a while</h2>

<p>The first scan on an established project is usually uncomfortable. That’s the tool working — it’s showing you a thing that was already true and simply wasn’t visible.</p>

<p><a href="https://marketplace.visualstudio.com/items?itemName=clawman.clawman">Install for VS Code</a>, open the panel, and see what’s in there.</p>]]></content><author><name></name></author><category term="release" /><summary type="html"><![CDATA[This started with a bad afternoon of debugging.]]></summary></entry></feed>